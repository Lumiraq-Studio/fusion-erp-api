name: Build and Deploy

on:
  pull_request:
    branches:
      - main
      - dev
  push:
    branches:
      - main
      - dev

jobs:
  # PR Build & Test only
  pr-build:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm run test

  # Docker build and artifact extraction
  build-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set image tag
        id: vars
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "TAG=${{ github.event.repository.name }}:${{ github.event.pull_request.number }}" >> $GITHUB_ENV
            echo "🔖 Using PR tag: ${{ github.event.repository.name }}:${{ github.event.pull_request.number }}"
          else
            echo "TAG=${{ github.event.repository.name }}:${{ github.sha }}" >> $GITHUB_ENV
            echo "🔖 Using commit tag: ${{ github.event.repository.name }}:${{ github.sha }}"
          fi

      - name: Build Docker image
        run: |
          echo "🚀 Building Docker image with tag: $TAG"
          docker build -f Dockerfile -t $TAG .

      - name: Run container to produce build artifacts
        run: |
          echo "📦 Running container to copy build artifacts..."
          mkdir -p build
          docker run --rm -v $(pwd)/build:/app/build $TAG
          echo "✅ Artifacts copied to ./build directory"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: app-build
          path: build

      - name: Clean up dangling images/containers
        run: |
          echo "🧹 Cleaning up Docker images and containers..."
          docker image prune -af
          docker container prune -f
          echo "✅ Cleanup complete"
